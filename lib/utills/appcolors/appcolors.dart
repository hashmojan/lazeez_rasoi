import 'package:flutter/material.dart';

class AppColors {
  // Primary theme colors
  static const Color primary = Color(0xFFD32F2F); // Deep Red
  static const Color primaryLight = Color(0xFFFF6659);
  static const Color accent = Color(0xFFFFC107); // Amber

  // Backgrounds & Cards
  static const Color background = Color(0xFFF9F9F9);
  static const Color card = Color(0xFFFFFFFF);

  // Text Colors
  static const Color textDark = Color(0xFF212121);
  static const Color textLight = Color(0xFF757575);

  // Category chips and highlights
  static const Color categoryChip = Color(0xFFEEEEEE);
  static const Color categoryChipSelected = Color(0xFFD32F2F);

  // Divider / Border
  static const Color border = Color(0xFFE0E0E0);

  // Favorite icon
  static const Color favorite = Color(0xFFFF1744);

  // Error
  static const Color error = Color(0xFFD32F2F);

  // Others
  static const Color shadow = Color(0x29000000); // 16% Black
}
